# Hard-coded constraints for maximum allowed qubits and allowed gates 

MAX_QUBITS = 5  

# Every valid gate keyword the user can write
ALLOWED_GATES = ["H", "X", "Y", "Z", "CNOT", "T", "S"]

# The keyword to start a program
INIT_KEYWORD = "INIT"